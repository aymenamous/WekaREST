version: '3'
services:
  mongo:
    image: mongo
    restart: always
  jguweka:
    image: aymmenn/jguweka:OAS3
    restart: always
    links:
      - "mongo"
    ports:
      - "8080-8085:8080"
    depends_on:
      - mongo
    labels:
      - "service-name:jguweka"
      - "service-type:app-srv"
      - "environment:test"
